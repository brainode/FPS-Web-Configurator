# Caddy reverse proxy for the Warsow control panel.
# Forwards all requests on port 80 to the control-panel service.
# For production HTTPS, replace :80 with your domain, e.g.: panel.example.com

:80 {
    reverse_proxy control-panel:8080
}
